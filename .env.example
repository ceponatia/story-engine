# Repo-level environment examples
NODE_ENV=development

# Database
DATABASE_URL=${vars.db.url}
REDIS_URL=${vars.redis.url}

# Deployment
DEPLOY_MODE=${vars.deploy.mode}
PUBLIC_DOMAIN=${vars.domain}

# Telemetry
TELEMETRY_VENDOR=${vars.telemetry.vendor}
