{
  "name": "@story-engine/domain-characters",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "pnpm -w exec tsc -b",
    "dev": "pnpm -w exec tsc -b --watch",
    "lint": "eslint src/",
    "type-check": "pnpm -w exec tsc -b --noEmit"
  },
  "dependencies": {
    "next": "^15.0.0",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}