import { render } from '@testing-library/svelte' import { test, expect } from 'vitest' import Cmp
from '$lib/features/{{kebab feature}}/entities/{{pascal name}}.entity.svelte' test('renders', () =>
{ const { container } = render(Cmp, {}) expect(container).toBeTruthy() })